Django backend scaffold for my-vue-app

This folder contains a minimal Django + DRF + simplejwt scaffold focused on authentication for the Vue SPA.

Instructions:
- Create a virtualenv and install dependencies listed in requirements.txt
- Run migrations and create superuser
- Use docker-compose.yml provided to run Postgres + Django in development
